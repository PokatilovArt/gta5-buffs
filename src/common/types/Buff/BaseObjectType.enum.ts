/**
 * Mirrors @altv/shared Enums.BaseObjectType values.
 * Defined locally to avoid runtime dependency on @altv/shared (v2 module).
 */
export enum BaseObjectType {
  PLAYER = 0,
  VEHICLE = 1,
  PED = 2,
}
